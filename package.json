{
  "name": "drive-dupe-destroyer",
  "version": "14.3.0",
  "private": true,
  "description": "Test harness for Drive Dupe Destroyer. The app itself is a dependency-free static site; this file exists only so `npm test` can run the node:test suite.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "PolyForm-Noncommercial-1.0.0"
}
